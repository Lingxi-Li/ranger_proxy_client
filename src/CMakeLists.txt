AUX_SOURCE_DIRECTORY(. PROXY_CLIENT_SOURCES)
ADD_EXECUTABLE(ranger_proxy_client ${PROXY_CLIENT_SOURCES})
TARGET_LINK_LIBRARIES(ranger_proxy_client
	${Boost_LIBRARIES}
	${OPENSSL_CRYPTO_LIBRARY}
)

INSTALL(TARGETS ranger_proxy_client DESTINATION bin)
